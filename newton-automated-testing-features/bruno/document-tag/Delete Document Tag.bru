meta {
  name: Delete Document Tag
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/mid/document-tag/{{tag_id}}
  body: none
  auth: none
}

headers {
  Cookie: access_token={{access_token}}
}

tests {
  test("Status is 200 or 204", function() {
    expect([200, 204]).to.include(res.getStatus());
  });
}
