meta {
  name: List Personal Doc Tags
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/mid/personal-doc-tag/list
  body: none
  auth: none
}

headers {
  Cookie: access_token={{access_token}}
}

params:query {
  ~limit: 
  ~offset: 
  ~search: 
}
